{
  "name": "@specflow/tauri",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:stack": "concurrently --raw -k \"npm run -w @specflow/app watch\" \"npm run -w @specflow/client dev\"",
    "dev": "tauri dev --config src-tauri/tauri.dev.conf.json",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.4"
  }
}
