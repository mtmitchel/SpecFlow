interface:
  display_name: "Architecture Reviewer"
  short_description: "Review changes against the repo architecture"
  default_prompt: "Use $architecture-reviewer to review this change or proposal against the documented architecture, component boundaries, and shared contracts."
